"""Implementations of the agent contract.

Scripted agents live here today; the language model agents join them in J7. They
are deliberately outside the engine package: the engine defines the contract,
it does not depend on who fulfils it.
"""
